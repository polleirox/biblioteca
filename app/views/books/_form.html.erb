<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.select :state, Book.states.keys %>
  </div>

  <div class="field">
    <%= form.label :date_in %>
    <%= form.date_select :date_in %>
  </div>

  <div class="field">
    <%= form.label :date_out %>
    <%= form.date_select :date_out %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
